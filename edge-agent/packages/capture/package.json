{
  "name": "@edge-agent/capture",
  "version": "1.0.0",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts --external opencv4nodejs --external sharp",
    "dev": "tsup src/index.ts --format esm --dts --watch --external opencv4nodejs --external sharp",
    "test": "vitest"
  },
  "dependencies": {
    "@edge-agent/common": "1.0.0"
  },
  "optionalDependencies": {
    "opencv4nodejs": "^5.6.0",
    "fluent-ffmpeg": "^2.1.2"
  },
  "devDependencies": {
    "@types/fluent-ffmpeg": "^2.1.21",
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0",
    "tsup": "^8.0.0",
    "vitest": "^1.0.0"
  }
}
