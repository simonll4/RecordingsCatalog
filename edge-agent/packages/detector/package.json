{
  "name": "@edge-agent/detector",
  "version": "1.0.0",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsup src/index.ts --format esm --external sharp && tsc -p tsconfig.json --declaration --emitDeclarationOnly --declarationMap false",
    "dev": "tsup src/index.ts --format esm --dts --watch --external onnxruntime-node --external sharp",
    "test": "vitest"
  },
  "dependencies": {
    "@edge-agent/common": "1.0.0"
  },
  "devDependencies": {
    "sharp": "^0.33.0",
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0",
    "tsup": "^8.0.0",
    "vitest": "^1.0.0"
  }
}
